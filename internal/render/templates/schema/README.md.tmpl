# schema

Schema definitions for `{{ .Name }}`.

Place JSON Schema, Protobuf `.proto` files, or OpenAPI/Swagger specs here. Keep one sub-directory per format:

```
schema/
  proto/     # .proto files
  openapi/   # openapi.yaml or swagger.json
  json/      # JSON Schema files
```
