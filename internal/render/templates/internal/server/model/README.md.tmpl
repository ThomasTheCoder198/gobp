# internal/server/model

Domain models for `{{ .Name }}`.

Models represent core business entities — framework-agnostic, no serialisation tags, no ORM annotations. Services operate on models; repositories convert between models and entities.
