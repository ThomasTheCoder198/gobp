# internal/server/repository

Data access layer for `{{ .Name }}`.

Repository structs wrap database queries. They accept a database connection in their constructor and return domain models or errors — never raw database types.

**Convention**: define a `XxxRepo` interface alongside `xxxRepo` struct. Wire them in `cmd/server/wire.go`.
