# internal/server/entity

Database entity definitions for `{{ .Name }}`.

Entities map directly to database tables and carry ORM/query tags. They are separate from domain models so that persistence concerns stay isolated — repositories are responsible for converting between the two.
